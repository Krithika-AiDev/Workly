class CollectorAgent:
    """
    Collector Agent collects raw user input.
    In a real system, this could be a chat interface, WhatsApp, or scheduled prompt.
    """
    def collect(self, text_input):
        return text_input
